#include <iostream>

int main() {
    int n;
    std::cin >> n;
    if (n == 0) {
        std::cout << 0 << std::endl;
        return 0;
    }
    while (n > 0) {
        std::cout << (n % 10);
        n /= 10;
    }
    std::cout << std::endl;
    return 0;
}
