#include <iostream>
using namespace std;

int main() {
    int n;
    std::cin >> n;
    if (n <= 1) {
        std::cout << "Number is not prime" << std::endl;
        return 0;
    }
    bool is_prime = true;
    for (int i = 2; i < n; ++i) {
        if (n % i == 0) {
            is_prime = false;
            break;
        }
    }
    if (is_prime) {
        std::cout << "Number is prime" << std::endl;
    } else {
        std::cout << "Number is not prime" << std::endl;
    }
    return 0;
}
